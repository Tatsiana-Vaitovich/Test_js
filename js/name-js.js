"use strict";
let nameJavaScript;
nameJavaScript=prompt("Какое официальное название JavaScript?",[]);
if (nameJavaScript==="ECMAScript") {
  alert("Верно!");
} else {
  alert("Не знаете? ECMAScript!");
}